/**
 * Tipos e regra de forma seguem @commitlint/config-conventional, que já bate com a
 * lista de .claude/rules/30-convencao-de-commits.md (feat, fix, docs, style,
 * refactor, perf, test, build, ci, chore, revert).
 */
module.exports = {
  extends: ['@commitlint/config-conventional'],
};
