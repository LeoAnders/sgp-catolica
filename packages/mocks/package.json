{
  "name": "@sgp/mocks",
  "version": "0.1.0",
  "private": true,
  "description": "Dados estaticos usados pelas telas da fase N1, no lugar de banco de dados e API.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "audit": "tsc -p tsconfig.audit.json && node scripts/auditar.cjs"
  },
  "dependencies": {
    "@sgp/shared-types": "*"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
